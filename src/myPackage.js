// @ts-check
/**
 * Initializes the project
 * @param {object} config
 * @param {boolean} config.debug
 * @param {string} config.url
 * @returns boolean
 */
// 타입스크립트 파일에게 자바스크립트 파일을 확인시킴
// 그저 코멘트이기 때문에 오류가 나지 않음

// export function init(config) {
//   return true;
// }

/**
 * Exits the program
 * @param {number} code
 * @returns number
 */

// export function exit(code) {
//   return code + 1;
// }
